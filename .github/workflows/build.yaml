name: build

on:
    pull_request:
        branches:
            - master

jobs:
    build:
        runs-on: [self-hosted, linux]
        steps:
            - uses: actions/checkout@v2
            - name: setup node
              uses: actions/setup-node@v2
              with:
                  node-version: "16"
            - run: yarn install
            - name: Check versions
              run: |
                  yarn --version
                  npx node --version
                  npx ts-node --version
            - run: yarn build
